---
mode: ask
---

Re-establish context for frontier-commerce after a reset or compaction:

1. Read `AGENTS.md`, then skim `.github/copilot-instructions.md`.
2. Read the newest entries in `docs/decision-log.md`.
3. Check `docs/working_memory/` for an in-flight task file; if present,
   resume from its "next action" after verifying `git status` and
   `git log --oneline -5` match what it claims.
4. Report: current branch, tree state, in-flight task (if any), and the
   next action you propose. Do not make changes yet.
